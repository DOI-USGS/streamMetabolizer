#' Convert a date to a day of year (1-366) with decimal hours
#'
#' Is there a way to import the documentation from the LakeMetabolizer package?
#'
#' cannotimportFrom LakeMetabolizer date2doy
date2doy <- function(x) {
  warning("need LakeMetabolizer access")
  #LakeMetabolizer:::date2doy(x)
}